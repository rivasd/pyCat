"""
A behavioral experiment examining the link between categorization performance and difficulty on resulting categorical perception

Django application using models from expData and expManager apps
client side implementation in jsPsych
"""

default_app_config = 'cat_experiment.apps.CatexperimentConfig'